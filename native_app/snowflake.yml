definition_version: 2
entity_model_version: 2

entities:
  cortex_cost_advisor:
    type: application package
    identifier: cortex_cost_advisor_pkg
    stage: app_src.stage
    manifest: app/manifest.yml
    artifacts:
      - src: app/*
        dest: ./
      - src: src/*
        dest: ./src/
    meta:
      role: accountadmin
      warehouse: compute_wh
      database: cortex_cost_advisor_pkg
      name: cortex_cost_advisor_pkg
