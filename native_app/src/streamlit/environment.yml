name: sf_env
channels:
  - snowflake
dependencies:
  # Warehouse/SiS Anaconda only accepts `=` pins and `*` ranges (not pip-style >=,<).
  - streamlit=1.35.0
  - pandas=2.*
  - snowflake-snowpark-python=1.*
  - altair=5.*
